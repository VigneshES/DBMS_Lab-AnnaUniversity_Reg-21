Notes:Type Start With 'SQL>'Command

1.Connect database
SQL> connect sys as sysdba;
Enter password:
Connected.

SQL>create user uname identified by pass;

SQL>grant connect to uname;

SQL>grant all privileges to uname;

SQL>connect uname; 



2.Create Table:
SQL> create table customer(NAME VARCHAR(20),GENDER VARCHAR(6),ADDRESS VARCHAR(20));

Table created.



3.Insert 5rows into table:
SQL> insert into customer values('Gugan','Male','Chennai');
1 row created.
SQL> insert into customer values('Salamon','Male','Kolkata');
1 row created.
SQL> insert into customer values('Bharath','Male','Kerala');
1 row created.
SQL> insert into customer values('Mubeer','Male','Mumbai');
1 row created.
SQL> insert into customer values('Subramani','Male','Andra');
1 row created.
SQL> insert into customer values('Vignesh','Male','Delhi');
1 row created.
SQL> select * from customer;

NAME                 GENDER PLACE
-------------------- ------ --------------------
Gugan                Male   Chennai
Salamon              Male   Kolkata
Bharath              Male   Kerala
Mubeer               Male   Mumbai
Subramani            Male   Andra
Vignesh              Male   Delhi




4.Procedure for inserting:
SQL> set serveroutput on
SQL> create or replace procedure insertcustomer(p_name customer.name%type,p_gender customer.gender%type,p_address customer.address%type)
is
begin
insert into customer(name,gender,address)
 values(p_name,p_gender,p_address);
 commit;
 end;
 /
Procedure created.

SQL> execute insertcustomer('Jepril','Male','bhopal');

PL/SQL procedure successfully completed.

SQL> select * from customer;

NAME                 GENDER ADDRESS
-------------------- ------ --------------------
Gugan                Male   Chennai
Salamon              Male   Kolkata
Bharath              Male   Kerala
Mubeer               Male   Mumbai
Subramani            Male   Andra
Vignesh              Male   Delhi
Jepril               Male   bhopal

7 rows selected.




3.Procedure For Updateing:
SQL> create or replace procedure updatecustomer(p_name in customer.name%type,p_gender in customer.gender%type,p_address in customer.address%type)
  is
  begin
  update customer set gender=p_gender,address=p_address where name=p_name;
  commit;
  end;
  /

Procedure created.

SQL> execute updatecustomer('jepril','Male','Karnada');

PL/SQL procedure successfully completed.

SQL> select * from customer;

NAME                 GENDER ADDRESS
-------------------- ------ --------------------
Gugan                Male   Chennai
Salamon              Male   Kolkata
Bharath              Male   Kerala
Mubeer               Male   Mumbai
Subramani            Male   Andra
Vignesh              Male   Delhi
Jepril               Male   bhopal

7 rows selected.



procedure forr deleting:
SQL> create or replace procedure deletecustomer(p_name customer.name%type)
  is
  begin
  delete from customer where name=p_name;
  end;
   /

Procedure created.

SQL> execute deletecustomer('Jepril');

PL/SQL procedure successfully completed.

SQL> select * from customer;

NAME                 GENDER ADDRESS
-------------------- ------ --------------------
Gugan                Male   Chennai
Salamon              Male   Kolkata
Bharath              Male   Kerala
Mubeer               Male   Mumbai
Subramani            Male   Andra
Vignesh              Male   Delhi

6 rows selected.







procedure for Selecting:

SQL> set serveroutput on
SQL> create or replace procedure selectcustomer(p_name customer.name%type)
  is
  p_address varchar(20);
  begin
  select address into p_address
  from customer where name='p_name';
  dbms_output.put_line('p_name='||p_name);
  dbms_output.put_line('p_address='||p_address);
   commit;
   end;
   /

Procedure created.









 










 




